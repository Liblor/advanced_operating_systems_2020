#include <stdio.h>

#include <aos/debug.h>

int main(int argc, char *argv[])
{
    printf("Starting %s\n", argv[0]);

    /* TODO: When a UDP datagram is received, echo it to the sender. */

    return EXIT_SUCCESS;
}
